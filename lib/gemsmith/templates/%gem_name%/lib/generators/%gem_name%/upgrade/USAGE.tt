Description:
    Upgrades previously installed <%= config.dig(:gem, :class) %> resources.

Example:
    rails generate <%= config.dig(:gem, :name) %>:upgrade

    This will create:
        what/will/it/create
